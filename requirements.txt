pypdf>=5.0
pytest>=8.0

# Chỉ cần khi chạy provider gemini thật (runtime):
# google-genai>=1.0

# Hồ sơ Digitization Manager (offline local UI)
fastapi>=0.115
httpx>=0.28
jinja2>=3.1
uvicorn>=0.30
pyinstaller>=6.0
