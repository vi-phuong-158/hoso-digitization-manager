pypdf>=5.0
pytest>=8.0

# Chỉ cần khi chạy provider gemini thật (runtime):
# google-genai>=1.0

# H? so Digitization Manager (offline local UI)
fastapi>=0.115
jinja2>=3.1
uvicorn>=0.30
pyinstaller>=6.0

# FastAPI/Starlette TestClient dependency for Manager integration/E2E tests
httpx2>=0.1
