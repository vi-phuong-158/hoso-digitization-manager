"""Offline Hồ sơ Digitization Manager.

The package is deliberately separate from the existing document-processing
pipeline in :mod:`app`; it stores metadata only and treats source files as
read-only.
"""

__version__ = "0.1.0"
